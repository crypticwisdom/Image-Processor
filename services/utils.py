import requests
from django.conf import settings

BASE_URL: str = settings.
def image_processor(client_token, block_token, images):
    response = requests.post(url='')


class ImageProcess:
    def __int__(self, client_token, block_token):
        ...